if has('win32')
	echoerr 'Win32 Defined'
endif
if has('win64')
	echoerr 'Win64 Defined'
endif
if has('gui')
	echoerr 'GUI Defined'
endif
if has('perl')
	echoerr 'perl works'
endif
if has('lua')
	echoerr 'lua works'
endif
if has('gui_macvim')
	echoerr 'GUI MacVim Defined'
endif
